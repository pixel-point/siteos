interface:
  display_name: "SiteOS Integrations"
  short_description: "Connect GitHub repositories and shared providers"
  default_prompt: "Use $siteos-integrations to connect the requested provider or selected GitHub repositories to the SiteOS Organization and verify its supported connection state."

policy:
  allow_implicit_invocation: true
