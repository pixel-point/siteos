interface:
  display_name: "SiteOS Trace"
  short_description: "Inspect destinations, tracking issues and safe evidence"
  default_prompt: "Use $siteos-trace to investigate destinations, events and tracking issues in the selected Project environment, then verify any requested fix using fresh evidence."

policy:
  allow_implicit_invocation: true
