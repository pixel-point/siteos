interface:
  display_name: "SiteOS Trace"
  short_description: "Investigate tracking and configure useful event checks"
  default_prompt: "Use $siteos-trace to study the selected website's tracking, derive requested event checks from observed behavior, use Google Analytics MCP reports when relevant, and verify changes with fresh evidence."

policy:
  allow_implicit_invocation: true
