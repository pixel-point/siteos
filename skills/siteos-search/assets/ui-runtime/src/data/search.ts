import type { SiteOSProjectSearchDialogItem } from "@/lib/siteos-project-search-dialog";

// Project-owned optional recents/suggestions for the dialog before the first query.
export const recentSearches: SiteOSProjectSearchDialogItem[] = [];
export const suggestions: SiteOSProjectSearchDialogItem[] = [];
