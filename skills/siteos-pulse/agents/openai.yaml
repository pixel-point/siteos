interface:
  display_name: "SiteOS Pulse"
  short_description: "Configure Playwright monitoring and GitHub PR checks"
  default_prompt: "Use $siteos-pulse to configure scheduled and GitHub PR Checks for the selected Project environment, validate locally and publish only when requested."

policy:
  allow_implicit_invocation: true
