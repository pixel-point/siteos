interface:
  display_name: "SiteOS"
  short_description: "Route setup and cross-service SiteOS work"
  default_prompt: "Use $siteos to inspect this SiteOS request, select only the required service skills, and keep Pulse, Forms, and Search Projects independent."

policy:
  allow_implicit_invocation: true
